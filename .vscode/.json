{
    
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build C++ to bin",
            "type": "shell",
            "command": "g++",
            "args": [
                "${file}",
                "-o",
                "${workspaceFolder}/bin/${fileBasenameNoExtension}.exe"
            ],
            "group": "build",
            "problemMatcher": [],
            "detail": "Compiles the open C++ file and places exe in bin/"
        }
    ]
}
